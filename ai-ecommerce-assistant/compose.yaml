# 本地完整栈：PostgreSQL 17 + Web + Worker（TASK-001；Gate-01 H10 修复认证配置）
# 前置：cp .env.example .env 并填写 BETTER_AUTH_SECRET（随机 64 hex）；
#       数据库口令由 POSTGRES_PASSWORD 提供（默认值仅限本机回环开发栈）。
# 启动：docker compose up -d --build
# 地址：Web http://127.0.0.1:3000 ；健康检查 http://127.0.0.1:3000/api/health
# 状态：docker compose ps（worker healthcheck 使用 dist/jobs/status.js）
# 迁移：容器内无 pnpm（corepack 需联网），在宿主对暴露的 PG 端口执行：
#   DATABASE_URL=postgresql://aiea:<POSTGRES_PASSWORD>@127.0.0.1:5433/aiea_dev \
#     pnpm exec prisma migrate deploy
# 初始化 Owner（镜像内含 dist/scripts bundle，无 pnpm 依赖）：
#   docker compose exec -T web sh -c "printf '%s' '<密码>' | node dist/scripts/init-owner.js --org 组织 --email a@b.c --name 显示名"
# M05：PostgreSQL/Web/Worker 统一引用同一 POSTGRES_PASSWORD；Web/Worker 通过
#       PG* 分量注入连接参数（应用侧组装连接串并对口令做 URL 编码），口令含
#       URL 保留字符（@ : / # ? % 等）也可用，无需手工转义。
# G2-H09：Web/Worker 挂载同一私有存储卷（.data/private），上传→Worker 解析→
#       查询/下载→容器重启读回共享同一对象；.data 不进 Git 与镜像上下文。
name: ai-ecommerce-assistant

services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: aiea
      # M05：本地开发口令可由 .env 覆盖（POSTGRES_PASSWORD）；默认值仅限本机回环开发栈
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-aiea_local_dev}
      POSTGRES_DB: aiea_dev
    ports:
      # M05：仅绑定回环，避免开发库暴露到可达网络
      - "127.0.0.1:5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aiea -d aiea_dev"]
      interval: 5s
      timeout: 3s
      retries: 12

  web:
    build: .
    environment: &app-env
      # M05：与 postgres 服务同一口令变量；应用与 prisma CLI 经 src/lib/dbUrl 组装连接串
      PGHOST: postgres
      PGPORT: "5432"
      PGUSER: aiea
      PGPASSWORD: ${POSTGRES_PASSWORD:-aiea_local_dev}
      PGDATABASE: aiea_dev
      # TASK-003 起启动必需（instrumentation 校验）；从 .env 注入，缺失即拒绝启动
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:?BETTER_AUTH_SECRET is required (set it in .env)}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL:-http://localhost:3000}
      LOG_LEVEL: info
      # G2-H09：私有存储运行变量（local 驱动；oss 为 TASK-007/M04 显式适配）
      STORAGE_DRIVER: ${STORAGE_DRIVER:-local}
      STORAGE_PRIVATE_ROOT: /app/.data/private
    volumes:
      # G2-H09：Web 与 Worker 共享持久私有存储卷（任务文件跨进程、跨重启可读）
      - private-data:/app/.data/private
    ports:
      - "127.0.0.1:3000:3000"
    depends_on:
      postgres:
        condition: service_healthy

  worker:
    build: .
    command: node dist/jobs/worker.js
    environment:
      # G2-H09：与 Web 同一运行环境（含认证/存储变量），不再缺 auth 配置
      <<: *app-env
    volumes:
      - private-data:/app/.data/private
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "dist/jobs/status.js"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

volumes:
  pgdata:
  private-data:
