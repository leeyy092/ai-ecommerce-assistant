# AI 电商运营助手｜独立技术审查报告

审查日期：2026-09-11  
审查角色：Codex，独立技术 Reviewer  
审查性质：开发前文档、数据契约与架构审查；本报告中的修改均为建议，尚未应用到原规格。  
本轮边界：未初始化应用、未修改业务代码或原规格、未运行 TASK-001、未调用收费模型、未部署或上传企业数据。

## 一、总体结论

**READY WITH FIXES——完成指定契约修订后，可以按阶段进入开发。**

现有技术方向适合受邀企业试点：Next.js 单应用、PostgreSQL、同仓库独立 Worker、CSV/Mock 共用 Adapter、确定性指标和规则、单模型结构化生成。保留这一方向，不增加 Redis、第二套队列、微服务、向量库、RAG 或 Agent 框架。

但不接受 Zcode 对“没有其他重要缺失”“只有约半天文档小修”“数据链路已经完全自洽”的判断。现有文件至少存在六组需要先明确的契约问题，涉及时间推进后的快照身份、VOC 与商品的快照读模型、告警子通道与客服权限、数据来源及配置变化、历史快照清理、后台失败恢复。它们会影响后续表结构或业务行为，不能全部推到页面联调和最终验收时解决。

**TASK-001 单独具备技术开工条件。** 它只搭建本地 Web、Worker、PostgreSQL 和检查环境，不建立业务表、不接入收费模型。因此，下文问题不需要阻塞这个环境任务；但应在进入相应业务 TASK 前关闭。本轮用户明确要求“先审查，等确认”，所以本报告不构成启动开发的授权，也不执行交接文档内写给 Zcode 的启动指令。

### 审查范围与证据边界

| 对象 | 本次检查结果 |
|---|---|
| `DEVELOPMENT_HANDOFF.md` | 全文阅读，核对 P0 白名单、TASK-001 执行包、依赖表和文档优先关系 |
| `01_PRODUCT_VISION.md`—`12_PROGRESS.md` | 全文阅读，覆盖产品、权限、页面、模型、指标、AI、规则、API、TASK、验收和工程规范 |
| `00_COMPLETE_SPEC.md` | 完整核对：PART 1—25 与相应分册逐段文本一致；额外阅读合订本前言及 PART 26。没有仅抽查合订本 |
| `AI电商运营助手_产品与工程规格_v1.0.zip` | ZIP 完整性检查通过；内含 13 份 Markdown 与当前目录相应文件逐字节一致 |
| Zcode《开发前诊断报告》 | 全文阅读；以下覆盖 H/M/L、缺失项、冲突、过度设计、范围及 Go/No-Go 建议 |
| 代码、数据库、配置、目录 | 工作区存在 Git 仓库；没有应用目录、应用代码、实际 Prisma Schema、迁移、package.json、lockfile、README 或应用环境配置。不能把逻辑模型当作已建数据库 |
| 图豆 AI 调研报告 | 属于另一个产品的调研资料；交接包明确不作为本项目开发输入，未混入本次产品结论 |

本次静态检查还确认：30 个 TASK 的现有依赖没有环；AI 文档内 5 个 JSON 代码块均可解析，Schema 的 23 处本地 `$ref` 均可解析到定义；发现页面字段表与 Schema 的一个字段名不一致。上述检查不是 Ajv 执行、数据库迁移测试、模型质量评测或性能测试。项目尚未实现，本轮没有可运行的应用测试套件，也没有证据支持“无高危漏洞”“真实企业可直接使用”或具体完工日期。

### 结论与优先级说明

- **ACCEPT**：接受原意见，必须在相应开发 TASK 前修订指定内容。
- **MODIFY**：问题或方向有价值，但事实依据、解决方案、优先级或执行方法需要修改。
- **DEFER**：当前无需作为 P0 开发前门槛；必要时在开发中补充或后置。
- **REJECT**：不采纳该项建议或其关键判断。

“高/中/低”表示审查处理优先级，不是新增产品阶段。“阻塞 P0”分别说明阻塞哪个业务 TASK 或真实试点；不能把“阻塞后续业务实现”解释为“连 TASK-001 都不能做”。

## 二、Zcode 建议审查表

### 2.1 全部意见索引

Zcode 没有为“缺失内容”“过度设计”“范围建议”和总评编号，以下分别补记 D、O、S、G，便于追踪。重复意见保留映射，不重复计算为新问题。

| Zcode 建议 | Codex 结论 | 优先级 | 处理方式 |
|---|---|---|---|
| H1 告警继承处理状态或增加忽略天数 | MODIFY | 中 | 仅考虑同期间、同规则语义、同证据的保守延续；不做跨日静默或通用忽略天数 |
| H2 最近 N 版＋被 AI 引用版本，异步清理 | MODIFY | 高 | 先定义精简的热快照与证据保留；不能因少量引用永久钉住整店全量版本 |
| H3 四个 TASK 超出单会话，按文件/区域分段 | MODIFY | 中 | 允许同一 TASK 跨会话；按可验收契约切段，共用内核先行，不建六套导入逻辑 |
| H4 限流完全没有归属 TASK | MODIFY | 中 | 纠正事实：TASK-029 已有“限流”；把具体实现前移到所属服务，029 做上线复核 |
| M1 TASK-017 改依赖 TASK-004 并提前 | MODIFY | 中 | 可提前做独立网关与门禁任务；采用单任务顺序执行，同步三处任务表，后续仍做真实业务集成 |
| M2 金额阈值唯一化到 RuleConfig | MODIFY | 低 | 04 §10.7 已限定 Store.settings；按现有裁决修正前面的摘要行，不再重新拍板 |
| M3 简化 AIRun 为累计估算＋硬停 | REJECT | 高 | 保留原子预占、每次外呼记录、未知费用占用和结算；不因月均费用低删除硬限额机制 |
| M4 明确店铺串行与 pg-boss 并发 | MODIFY | 高 | 同时明确数据库锁、RR 并发异常重试及过时任务处理，不只写队列并发数字 |
| M5 增加审计查询 SQL 或内部端点 | MODIFY | 低 | 只做受限只读 SQL/命令，不增加管理 API 或审计工作台 |
| L1 客服可见 customer_service＋去金额的 after_sales | MODIFY | 高 | 以 rule/subchannel 和证据白名单授权；整类 after_sales 不能直接放行 |
| L2 固定负面词表的文件位置未定 | DEFER | 低 | 词表及规则已存在；TASK-018 选一个版本化常量文件即可，不放可编辑 RuleConfig |
| L3 指定 Decimal 和日期库默认值 | DEFER | 低 | 在相关 TASK 选择一次、集中封装并锁版本；不是开发前业务决策 |
| L4 补数据源 PATCH、别名 GET | DEFER | 低 | 没有对应 P0 操作就不补完整 CRUD；保留现有未知 SKU 的明确映射路径 |
| L5 汇总行级错误与 HTTP 错误关系 | ACCEPT | 中 | 补引用及同步/异步差别，不能把任务失败一律变为查询接口 422 |
| L6 复合外键和部分唯一索引都须 raw SQL | MODIFY | 中 | 复合关系可以用 Prisma 表达；仅选定版本无法稳妥表达的约束用受审查 SQL migration |
| D1 快照清理缺失 | MODIFY | 高 | 同 H2，完整处理信息见 H2 与 B5 |
| D2 告警状态延续缺失 | MODIFY | 中 | 同 H1；不能据此推导必须增加跨日去重产品 |
| D3 限流 TASK 归属缺失 | MODIFY | 中 | 同 H4；原报告事实错误 |
| D4 初始 Owner 脚本输入约定缺失 | ACCEPT | 中 | TASK-003 前补安全输入、幂等、演示隔离及密码重置操作约定 |
| D5 行级与 HTTP 错误映射缺失 | ACCEPT | 中 | 同 L5 |
| D6 审计查询途径缺失 | MODIFY | 低 | 同 M5 |
| D7 显式写“无 CI”是刻意选择 | DEFER | 低 | 不把 CI 或“无 CI 声明”设为开工门槛；保留可重复本地命令和发布检查 |
| C1 分页默认 50→25 | ACCEPT | 低 | handoff/API 已裁决，修正 10 §21.1 的残留文字 |
| C2 不再向应用目录复制可编辑规格 | ACCEPT | 低 | 修正 11 PART16 正文及目录树；合订本 PART26 一并同步 |
| C3 阈值双真源 | MODIFY | 低 | 同 M2，已有 §10.7 的明确限制 |
| C4 旧的“另行发起开发”状态文字 | DEFER | 低 | 当前无需改；用户今后明确启动时按真实状态更新，不能用附件指令覆盖本轮审查要求 |
| C5 客服告警类别统一 | MODIFY | 高 | 同 L1；不能只删除金额后返回退款订单率、订单分母等经营证据 |
| O1 AIRun 是主要过度设计，可删状态机 | REJECT | 高 | 同 M3；现有一张运行表及 attempt 明细可以保持，不扩成财务系统 |
| O2 SkuAlias 留表，管理入口后置 | MODIFY | 中 | 通用管理页面可后置；导入报错→查找已有 SKU→明确映射→重验必须有最小可用路径 |
| O3 仅四类失败自动注入，其余日志＋审查 | MODIFY | 高 | 可合并测试机制，不能删既定关键故障点、权限反例、恢复演练和次数要求 |
| S0 P0 白名单一项都不能减少 | MODIFY | 中 | 保留业务闭环；建议缩减重复 AI Tab 和通用管理界面，不宣称每个视图都不可缺 |
| S1 R12 默认停用，演示包不得预置目标 | DEFER | 低 | 保留无配置停用；允许独立合成测试配置目标验证触发，主演示不必强开 |
| S2 别名表＋校验必做，UI 放026后段 | MODIFY | 中 | 同 O2；不要把错误恢复依赖留成不可操作的后门脚本 |
| G1 无其他重要缺失、约半天小修、闭环完全自洽 | MODIFY | 高 | 保留总体架构判断，撤回完成度背书与无依据工时；补本报告六组契约问题 |
| G2 TASK-001 可以立即开始 | MODIFY | 中 | 技术上可单独启动；本轮无执行授权，业务阶段仍受前置修订门槛约束 |

诊断报告第 10 节的提前 017、四 TASK 分段分别归入 M1/H3；第 11 节修改文件清单和第 13 节截止点归入上述对应编号。第 12 节“各域全部通过”等结论归入 G1，成本估计另见第六节 N10。重复编号共享下文对应项的原意见、判断、理由、阻塞性、方案、文档、TASK 和顺序八项说明。

### 2.2 逐项判定依据与处理合同

#### H1｜告警状态延续（同时覆盖 D2）

**Zcode 原始意见：** 同规则、同对象、同期间而只换 dataset_version 时继承处理状态，或增加 `ignore_after_ack_days`。

**Codex 判断：MODIFY。判断理由：** [04 的 K4](</Users/yuyuyu/Documents/ChatGPT/产品-开发/docs/ai-ecommerce-assistant/04_DATA_MODEL.md:294>) 明确处理状态只属于该版本。原报告指出的重复确认体验问题可能成立，但不能只凭四元组继承：更正订单或退款后，同一天的 warning 可能已经升成 critical，旧的 ignored 会掩盖新事实。这个方案也不会解决“第二天 period 已改变”的日常重复，原报告混合了两种问题。

**是否阻塞 P0：** 不阻塞 TASK-001；跨日静默不是 P0 必需。若做延续，在 TASK-016/024 前定清楚。

**推荐处理方案：** 不新增忽略天数、批量处理或告警生命周期系统。P0 默认新日期重新检测。建议仅当同租户/店铺/规则/对象/子通道/期间、规则参数与等级相同，且规范化证据内容相同，才复制 acknowledged/ignored 作为处理记录；写出来源告警和审计。证据变化则 open；resolved 不自动复制。先实现这条保守边界，跨日降噪留待真实使用证据。

**修改文档：** 04 Alert/K4、07 共同处理状态说明、10 对应回归案例。**影响 TASK：** 016/024 增加同证据和升级证据两个对照检查。**开发顺序：** 不改主顺序。

#### H2｜快照保留策略（同时覆盖 D1）

**Zcode 原始意见：** 只保留最近 N 个已发布版本和被 AIReport/AIInsight 引用的版本，发布后异步清理。

**Codex 判断：MODIFY。判断理由：** 存储增长风险成立，但“被引用的整版永久豁免”可能使每天一份历史日报把每天整店快照都保留下来，失去清理效果。按密集展开估算，10,000 SKU×90天×90版，单一 SKU 指标就是 8,100 万行；三个指标是 2.43 亿行。这是最坏实现形态的算术估算，不是当前真实行数或性能结论。VOC 每批改版会进一步放大构建工作。

**是否阻塞 P0：** 不阻塞 001；快照构建/清理合同在 013 前必须确定，否则既定试点规模没有可审查的存储边界。

**推荐处理方案：** 默认只保留当前、上一完整快照以及在途任务正在使用的构建版本；历史 AIReport/AIInsight 保留自身完整的脱敏证据包和必要引用记录，不因少量证据永久保留整店所有指标。清理前确认引用闭合；不能删到历史详情依赖的事实证据。优先稀疏保存实际需要的 SKU 日聚合，由覆盖和完整目录区分“可证明零”与“缺失”，不为每个未售 SKU/每一天/每个版本复制零行。增加定期小批清理，失败不回滚当前发布；不加分库、分区体系或 Redis。详细闭合条件见 B5。

**修改文档：** 04 §10.4/11.3、10 §21.1/PART23、11 §14.2/14.3。**影响 TASK：** 013/014/016/018/020/029。**开发顺序：** 不调整编号；保留策略应先于快照服务，不能等029才设计。

#### H3｜任务体量与会话边界

**Zcode 原始意见：** 008/016/021/026 各拆成内部阶段，不增编号，008 按六种文件逐段交付。

**Codex 判断：MODIFY。判断理由：** 这些任务确实需要检查点，但没有实际编码证据支持“必定 2—4 会话”。“每次只做一个 TASK”不等于“一个 TASK 必须一个会话完成”。六种文件应共享解析、校验、错误和 staging 内核，逐文件独立复制会增加维护成本。

**是否阻塞 P0：** 不阻塞；开始对应大 TASK 前写清检查点即可。

**推荐处理方案：** 008 按公共映射/覆盖/staging→六种字段规则与关联校验→预览/过期/冲突划分；016 按规则注册表与子通道→规则正反边界→发布集成划分；021 先确定性数据和状态，再接 AI/行动；026 先上传到提交，再做历史恢复。跨会话仍保持同一 TASK 为 IN_PROGRESS，记录已通过/未通过步骤，只有整体合同通过才 DONE。

**修改文档：** 09 四个任务、11 第5条和会话交接、12 记录格式。**影响 TASK：** 008/016/021/026，范围不增加。**开发顺序：** 保持原依赖；内部步骤明确化。

#### H4｜限流归属（同时覆盖 D3）

**Zcode 原始意见：** 没有任何 TASK 包含限流，应加入003、008或026、019。

**Codex 判断：MODIFY。判断理由：** [09 TASK-029 修改范围](</Users/yuyuyu/Documents/ChatGPT/产品-开发/docs/ai-ecommerce-assistant/09_TASKS.md:351>) 已明确写“限流”。错误在于诊断把晚实现说成没归属。把确认接口的保护放到纯前端026也不合适，且遗漏了 `/reports/generate`。

**是否阻塞 P0：** 不阻塞001；对应写接口开放给真实用户前必须完成，安全要求不能后置到试点之后。

**推荐处理方案：** 登录及邀请防爆破归003；上传资源上限归007；导入确认/重试请求保护归009及对应服务；手动洞察和日报入口在019/020共享组织配额和生成限流；029复核代理后的真实客户端地址、返回等待时间与生产行为。沿用数据库和认证库，不新增限流服务。

**修改文档：** 09 对应任务输出/验收、08 429响应、10 PART23。**影响 TASK：** 003/007/009/019/020/029。**开发顺序：** 责任前移，不改主任务编号。

#### M1｜提前 TASK-017

**Zcode 原始意见：** 网关、Schema、AIRun 只依赖004，可以与005—016并行，缩短关键路径。

**Codex 判断：MODIFY。判断理由：** 提前暴露供应商及结构化输出风险有价值，017 的网关和校验入口可用002已建表、004权限及真实PG合成证据测试完成。但单开发 Agent 的顺序调整不等于自动节省一个 TASK 的总工时；handoff/12 又明确只能同时一个 IN_PROGRESS，不能只改09然后并行开工。

**是否阻塞 P0：** 不阻塞；属于经确认后的排期优化。缺密钥不阻塞001，但会阻塞017的真实模型合同验收。

**推荐处理方案：** 建议 001→002→003→004→017→005…016→018…030。017只交付调用入口、结构/候选证据校验、原子预算和测试，不提前编写 EvidenceBuilder、VOC、日报或页面。其真实PG证据用合成测试记录，不用伪成功接口；019再验证实际聚合输出与证据构建，030保留联网质量门槛。若017缺必需账号输入，明确 BLOCKED，不能用stub冒充完整完成；是否恢复原顺序作为一次明确排期变更记录。

**修改文档：** handoff §6、09 TASK-017、12依赖表同时修订，06引用补充验证边界。**影响 TASK：** 017前移；018/019保留集成责任。**开发顺序：** 见第八节；不并行启动多个TASK，不新增任务号。

#### M2｜阈值真源（同时覆盖 C3）

**Zcode 原始意见：** Store.settings 和 RuleConfig.parameters 双重存阈值，需要选择后者。

**Codex 判断：MODIFY。判断理由：** [04 §10.7](</Users/yuyuyu/Documents/ChatGPT/产品-开发/docs/ai-ecommerce-assistant/04_DATA_MODEL.md:464>) 已明确 Store.settings 只允许 required_channels/authoritative_source_ids。因此是同文档旧摘要未同步，不是仍需产品拍板的双真源。

**是否阻塞 P0：** 不阻塞001；无新的架构决定。

**推荐处理方案：** 仅删除 Store 表 settings 描述中的“阈值金额”；R05/R12金额和单位仍只存 RuleConfig.parameters，并遵循按币种配置和无配置停用。

**修改文档：** 04 Store.settings 摘要行；必要时07增加交叉引用。**影响 TASK：** 005/016按既有规则执行，无新增工作流。**开发顺序：** 不变。

#### M3｜AIRun 预算状态（同时覆盖 O1）

**Zcode 原始意见：** 为每日3元预算维护预占、结算、unknown和attempt明细偏重，可改为成本累计＋硬停，节省约三分之一017工时。

**Codex 判断：REJECT。判断理由：** 实际范围允许日预算20元、月预算500元，也包含超时重试、多个店铺、手动和自动入口。只在调用前读累计费用并不能保证硬停；网络超时也不能证明没有扣费。把未知usage“按已花费计”仍需要持久的上限预占和逐attempt记录，否则不知道该计多少、何时释放、重启后是否重复调用。三分之一的节省没有实测依据。

**是否阻塞 P0：** 原合同不阻塞；若采纳删除预占的方案，会破坏P0费用控制验收。

**推荐处理方案：** 保留一张AIRun及attempt JSON、一段Organization锁内预占/结算事务、有限重试。unknown只保守占用，不自动释放或谎称实际费用已知。开发时明确预算周期归属和跨日重试，不建设账单平台、充值或供应商对账后台。

**修改文档：** 无需因本建议改04/06/handoff计费合同；开发说明可补周期边界。**影响 TASK：** 017/019/020/027沿用原验收。**开发顺序：** 不因“简化计费”调整。

#### M4｜并发发布协议

**Zcode 原始意见：** 把每店串行、全局并发与pg-boss配置对应起来。

**Codex 判断：MODIFY。判断理由：** 必须明确，但队列并发不替代数据库一致性。RR事务读取旧版本后，再锁已被别的事务更新的Store行，可能得到序列化失败，而不是正常读到新版本进入superseded分支。PostgreSQL官方要求此类失败回滚并重试整个事务。[PostgreSQL 17 事务隔离](https://www.postgresql.org/docs/17/transaction-iso.html)

**是否阻塞 P0：** 不阻塞001；013/016实现前必须有一个可执行协议。

**推荐处理方案：** 保留同店短提交锁和受限重算调度、全局有限并发；所有变更经营输入的入口都参加Store版本协议，包括导入、有效VOC标签、规则及覆盖要求修改、每日评估。RR只读一致输入，构建结果标清版本；发布使用短事务锁/CAS核对当前版本。若采用同一RR事务发布，明确捕获40001并在新事务记录superseded/安排最新任务，不在已失败事务内继续写状态。重试设上限，最新任务优先，避免持续小批VOC更新导致重算一直饥饿。

**修改文档：** 04 §11.1/11.3、11 §14.2/14.3、09 013/016、10并发恢复案例。**影响 TASK：** 009/013/016/018/020。**开发顺序：** 协议在013前定稿，不加另一套队列。

#### M5｜审计查询（同时覆盖 D6）

**Zcode 原始意见：** 提供只读SQL模板或最小内部端点。

**Codex 判断：MODIFY。判断理由：** 查询“谁何时改了什么”需要可操作路径，但不需要新增产品入口或扩大API攻击面。

**是否阻塞 P0：** 不阻塞开发；试点交接前必须能按组织/对象/时间查到审计。

**推荐处理方案：** 029给出受限只读参数化查询/命令和操作说明，输出操作者、对象、时间、脱敏前后摘要；不允许未经筛选的全租户导出。

**修改文档：** 09 TASK-029输出、10 PART23及未来运行说明。**影响 TASK：** 029。**开发顺序：** 不变。

#### L1｜客服告警白名单（同时覆盖 C5）

**Zcode 原始意见：** C可看customer_service和after_sales，后者去掉金额即可。

**Codex 判断：MODIFY。判断理由：** after_sales还可能含R03订单退款率、付款订单数和R10销售件数分母；没有金额也会暴露02禁止的经营统计。删除evidence中的金额也无法保证title/handling_note/样本/相关建议不泄露。

**是否阻塞 P0：** 不阻塞001；阻塞004权限映射和016/024客服告警实现，必须在相应任务前明确。

**推荐处理方案：** C只允许R08明确投诉/售后case计数子通道与R09的客服证据投影；R03/R10不返回给C。仍执行脱敏、字段白名单和混合证据整条拒绝。分类枚举只管展示，授权按规则/子通道和字段决定；处理备注也属于投影内容。

**修改文档：** 02矩阵补充、03告警页面、07规则映射、08 alerts GET/PATCH、10权限测试。**影响 TASK：** 004/016/019/024。**开发顺序：** 先明确权限，再写返回DTO，不能等024补。

#### L2｜固定关键词文件位置

**Zcode 原始意见：** voc-keywords-v1未指定代码常量还是RuleConfig，可能影响classification_version。

**Codex 判断：DEFER。判断理由：** [06 §8.3](</Users/yuyuyu/Documents/ChatGPT/产品-开发/docs/ai-ecommerce-assistant/06_AI_CAPABILITIES.md:43>) 已给词表、标准化、匹配方式和最小样本，并写与标签规则一起存放。文件名属于普通实现选择；关键词字典版本与模型分类版本也不应混成可编辑阈值。

**是否阻塞 P0：** 否。

**推荐处理方案：** 018使用一个版本化常量文件，关键词提取实现变化纳入既有计算/规则版本；P0不提供词库编辑、动态加载或新模型。

**修改文档：** 无开发前必改；018完成时在11/进度记录实际文件。**影响 TASK：** 018常规实现。**开发顺序：** 不变。

#### L3｜Decimal与日期工具库

**Zcode 原始意见：** 在11给默认库，避免会话间选型不一致。

**Codex 判断：DEFER。判断理由：** 业务要求已明确Decimal、IANA时区、UTC和168小时窗口，具体轻量库可以由实现者在现有栈内选择。没有必要再开一轮产品决策。

**是否阻塞 P0：** 否。

**推荐处理方案：** 在首次需要的006/014选择并锁定一个方案，集中在lib工具入口，后续复用；用跨午夜、DST和168小时正反案例验证，不把本地“加7天”默认当连续168小时。

**修改文档：** 11记录最终依赖；无需先修改指标公式。**影响 TASK：** 006/014。**开发顺序：** 不变。

#### L4｜数据源PATCH与别名GET

**Zcode 原始意见：** 因模型有disabled字段，API应考虑补数据源更新和别名列表。

**Codex 判断：DEFER。判断理由：** 一个状态字段不自动要求完整CRUD。当前设置页规定的是创建与覆盖查询，店铺归档已有入口；别名新增也不必配独立管理页。

**是否阻塞 P0：** 这两个完整管理端点不阻塞；未知SKU恢复是否可操作另按O2验收。

**推荐处理方案：** P0不显示未实现的数据源停用按钮，不增加任意来源迁移；未知SKU映射只提供所需查找和提交。确有正在执行任务需要识别disabled时，沿用后台有效性校验，不因此扩展管理工作台。

**修改文档：** 03/08明确本版可操作范围即可；无强制新增端点。**影响 TASK：** 005/026/027。**开发顺序：** 不变。

#### L5｜行级错误与HTTP信封（同时覆盖 D5）

**Zcode 原始意见：** 在08引用04 PART12的行级错误清单。

**Codex 判断：ACCEPT。判断理由：** 应统一引用，避免前后端发明不同错误名；还需区分同步请求失败和已接受异步任务失败。

**是否阻塞 P0：** 不阻塞001；008之前定清错误协议。

**推荐处理方案：** 请求参数错误返回相应4xx；上传已返回202后出现行错误，ImportTask为failed，GET任务本身仍可200，响应含任务错误码/行错误数量/错误文件。冲突返回409，超限413，不可重试的格式错误要求重传，禁止无限自动重试。单独列FILE_RETIRED、预览过期和安全stage，不回显原文。

**修改文档：** 08 §17.1/17.3引用04 PART12及10 PART13，增加最小映射。**影响 TASK：** 007/008/009/026。**开发顺序：** 不变。

#### L6｜Prisma与SQL约束

**Zcode 原始意见：** `(org_id,id)`复合外键及单Owner部分唯一索引需要raw SQL。

**Codex 判断：MODIFY。判断理由：** “复合外键必须raw SQL”不准确，Prisma官方文档提供多字段关系。部分索引支持又取决于锁定版本；官方v7.4已提供Preview支持，不能依据旧经验一概断言。[Prisma 多字段关系](https://docs.prisma.io/docs/orm/v7/prisma-schema/data-model/relations/one-to-many-relations)、[Prisma v7.4更新](https://www.prisma.io/changelog/2026-02-11)

**是否阻塞 P0：** 不阻塞001；002必须实际验证约束生成结果。

**推荐处理方案：** 用当前锁定版本能稳定表达的复合关系；Owner条件唯一、必要CHECK或不支持的关系用明确SQL migration。P0不为少一个SQL语句强行启用Preview。复合外键只能约束归属，金额合计、退款跨行上界等仍需锁内业务校验；不能写成一个CHECK就能保证全部跨表口径。

**修改文档：** 04 §10.1/身份完整性、09 TASK-002实现注意事项；无需改栈。**影响 TASK：** 002/003及导入事务测试。**开发顺序：** 不变。

#### D4｜Owner初始化与重置合同

**Zcode 原始意见：** 初始化脚本只有一句话，输入、演示组织创建方式不清楚。

**Codex 判断：ACCEPT。判断理由：** 需要一条可重复、无公开注册的真实启动路径；08已要求部署管理员密码重置，该流程也应有归属。

**是否阻塞 P0：** 不阻塞001；003前补约定，缺少可用Owner会阻塞所有后续权限和交互验收。

**推荐处理方案：** 输入组织名、Owner邮箱/显示名和组织演示标记；密码通过交互隐藏输入或受限秘密文件读取，不用明文命令参数或日志。原子建立Auth身份、领域User、Organization及Owner Membership；重复执行不覆盖既有密码或造第二个Owner。重置只由部署管理员核验身份后操作并撤销会话。演示初始化独立且醒目标识，不混入真实租户。

**修改文档：** 09 TASK-003、08认证附注、11未来scripts/README职责。**影响 TASK：** 003/028/029。**开发顺序：** 不变。

#### D7｜CI约定

**Zcode 原始意见：** 建议明确无CI是刻意选择。

**Codex 判断：DEFER。判断理由：** 当前没有应用或远程协作流程；写“无CI”本身不能提高验证质量。已有每TASK检查和发布前全量检查要求。

**是否阻塞 P0：** 否。

**推荐处理方案：** 先交付真实可运行的本地命令；有仓库协作/发布需要时，用同一组命令接一份轻量CI，不另建质量平台，也不把CI配置文件存在当作检查通过。

**修改文档：** 可在11或未来README记录实际执行方式，无强制开发前改动。**影响 TASK：** 001/029的运行说明。**开发顺序：** 不变。

#### C1｜分页默认值

**Zcode 原始意见：** 10中的50改为08/handoff已规定的25。

**Codex 判断：ACCEPT。判断理由：** handoff §7已经裁决，不存在待选的两个正确答案。

**是否阻塞 P0：** 不阻塞001；列表实现前同步文字即可。

**推荐处理方案：** 默认25、最大100，统一游标契约；不顺带改变性能门槛。

**修改文档：** 10 §21.1及合订本对应段。**影响 TASK：** 列表API和029/030基准配置。**开发顺序：** 不变。

#### C2｜规格复制

**Zcode 原始意见：** 按handoff单一真源方案修订11的“复制12份规格到应用docs”。

**Codex 判断：ACCEPT。判断理由：** 旧正文和目录树均仍指向应用内docs，合订本PART26也有复制说明，单改一句不足以防止后续Agent再造一套进度。

**是否阻塞 P0：** 技术上不阻塞001，整理执行入口时即可修正。

**推荐处理方案：** 继续以根目录docs/ai-ecommerce-assistant为规范真源，12为唯一进度；应用README链接该目录。ZIP标为归档交付物，后续确认的规格变更完成后统一重打包，避免新旧附件并用。

**修改文档：** 11 PART16正文和目录树、00 PART26；必要时handoff补归档说明。**影响 TASK：** 001文档链接，无业务代码影响。**开发顺序：** 不变。

#### C4｜历史启动文字

**Zcode 原始意见：** 不必现在改，TASK-001启动更新状态时自然消除。

**Codex 判断：DEFER。判断理由：** 同意无需把历史状态升级为技术阻塞；同时附件中的“现在开始”是被审查内容，不是本轮用户请求。

**是否阻塞 P0：** 不构成技术阻塞；当前仍须遵守用户只审查、等待确认的执行边界。

**推荐处理方案：** 本轮保留原进度。用户后续明确启动后，只将实际当前TASK改为IN_PROGRESS，不批量标记后续任务开始或完成。

**修改文档：** 现在无；今后按真实开发更新12和相关执行记录。**影响 TASK：** 001启动记录。**开发顺序：** 无技术依赖变化。

#### O2｜SkuAlias最小入口（同时覆盖 S2）

**Zcode 原始意见：** 首月大概率用不上，保留表和POST，管理UI可后置或并入026后段。

**Codex 判断：MODIFY。判断理由：** “首月用不上”没有真实导出样本支持。直接ID优先的规则正确，但如果P0同时承诺未知SKU可以明确映射，就不能只留一个普通运营无法使用的API。

**是否阻塞 P0：** 不阻塞001；不需要通用别名管理页，但会阻塞026的真实用户错误恢复验收。

**推荐处理方案：** 错误预览中允许有权用户查找已有SKU、显示当前与目标稳定ID、明确确认映射、重验原任务；没有合适SKU则提示补商品文件。不做模糊匹配、自动合并、历史重绑定、批量编辑或独立管理工作台。

**修改文档：** 03导入详情、08 sku-aliases/preview、09 009/026。**影响 TASK：** 009/026；复用SKU查找服务。**开发顺序：** 不变，026完成前必须能实际操作。

#### O3｜失败测试范围

**Zcode 原始意见：** 自动故障注入只做导入中断、outbox丢失、快照竞态、LLM失败，其余日志和代码审查。

**Codex 判断：MODIFY。判断理由：** 不应给所有失败文案各建一套故障系统，但10已经要求四个关键worker中断时点各重复3次，以及权限反例、备份恢复和029故障检查。“归入四类”不能变成跳过其中任一提交/确认窗口，也不能用日志存在替代验证。另经逐行计数，PART13实际列有25个失败场景，原诊断写23个不准确；数量本身不构成新增开发需求。

**是否阻塞 P0：** 不阻塞001；削减既定关键验证会阻塞工程发布。

**推荐处理方案：** 共用一套可控worker故障点和provider stub，分别验证事务前、事务中、提交后入队前、任务写结果后确认前等既定窗口，保留约定重复次数和真实PG断言；CSV结构错、权限、Schema反例用普通自动测试；表单文案/布局做必要行为测试和人工检查。恢复演练、磁盘/Worker/DB故障仍归029，不为每个场景重复实现框架。

**修改文档：** 09相关TASK说明、10 PART22的用例归组；不降低PART21门槛。**影响 TASK：** 013/016/017/020/029/030。**开发顺序：** 高风险内核随TASK测，发布前汇总，不在每次UI小改后重复全量压测。

#### S0｜是否原样保留全部P0呈现

**Zcode 原始意见：** 白名单不增不减，删除任何一项都会破坏试点可信度。

**Codex 判断：MODIFY。判断理由：** 数据、权限、原子导入和降级确实应保留；但五个展示同一数据的AI Tab或通用别名管理页不是验证付费的必要条件。不能把所有已写入文档的呈现方式都当承重功能。

**是否阻塞 P0：** 不阻塞；属于本报告提请确认的减量建议，不已生效。

**推荐处理方案：** AI工作台首版用一个建议列表、状态筛选和详情，已有字段足以呈现重点、原因、动作和优先级；五Tab中的重复页面组织后置。保留Dashboard、SKU、客服、导入和必要设置，各页共享服务与组件，不重做完整设计。

**修改文档：** handoff §2.3、01 P0呈现说明、03 AI工作台/路由、09 TASK-025；功能证据和个人状态不删。**影响 TASK：** 021/025减少UI装配工作。**开发顺序：** 不变。

#### S1｜R12演示配置

**Zcode 原始意见：** 无目标/金额配置就停用，演示包也不应预置目标。

**Codex 判断：DEFER。判断理由：** 停用默认已明确；但“不许在演示或测试数据配置目标”并非必要限制，否则无法验证已承诺的R12触发路径。

**是否阻塞 P0：** 否。

**推荐处理方案：** 真实店保持未配置停用；主演示按需要选择；单独合成测试配置明确目标和样本金额，覆盖触发/不触发/未成熟/无配置四类行为。不替客户推断盈利目标。

**修改文档：** 无开发前必改；028演示说明标清合成配置。**影响 TASK：** 016/028。**开发顺序：** 不变。

#### G1｜总体完整度与工时背书

**Zcode 原始意见：** 无Critical，无其他缺失，闭环可形成，问题均为约半天文档小修；多数技术域完整或安全。

**Codex 判断：MODIFY。判断理由：** 同意架构不需推倒；不同意把规范覆盖面等同于契约闭合或实现可用。本报告B1—B6需要明确实际字段/查询/状态行为，不能只补标题。也没有运行数据可以证明性能或“无高危漏洞”。

**是否阻塞 P0：** 具体阻塞见第三节；不是全项目推倒重做。

**推荐处理方案：** 把结论改为“静态审查有条件通过”，用可审查的契约差异和测试反例关闭问题；工期仍为待验证估计，首家样本接入工时单独记录。

**修改文档：** 本独立报告记录纠正；无需改写Zcode原报告。后续更新09排期说明和12实际证据。**影响 TASK：** 002起的相关内核任务。**开发顺序：** 在相应门槛处停止越过未解决合同。

#### G2｜TASK-001 Go/No-Go

**Zcode 原始意见：** TASK-001可以立即执行，上述问题都不触及其验收。

**Codex 判断：MODIFY。判断理由：** 技术判断对001成立，但“可做”不能代替用户本轮明确保留的执行确认，也不能外推为002—030可无条件连续开发。

**是否阻塞 P0：** 001没有业务规格阻塞；当前未获得本轮编码指令。

**推荐处理方案：** 本轮只交付报告。用户确认后可选择先落实契约小修，再启动001；也可单独授权001，完成环境后暂停在002前，先关闭表结构相关问题。

**修改文档：** 本报告第十节给出门槛；原进度暂不变。**影响 TASK：** 001严格只做环境，002另受B1/B2/B3约束。**开发顺序：** 见第八节。

## 三、必须开发前修改的问题

**仅以下六组属于当前需要先明确的P0业务合同。** “先修改”指先于表中受影响的实现，不要求把全部业务问题解决后才能安装本地基础环境。小文案差异、具体库名、CI、通用管理入口不列入阻塞清单。

| 编号 | 真正阻塞的问题 | 最晚关闭门槛 | 最小修改范围 |
|---|---|---|---|
| B1 | 时间变化会改变成熟度/规则，但现有快照身份只随事实或配置变化 | TASK-002前确定字段及版本语义；013/015/020实现 | 04/05/06/07/11中的评估时间、版本触发、幂等与缓存协议 |
| B2 | 发布快照不足以支撑SKU/渠道VOC筛选、部分摘要和历史证据一致读取 | TASK-002前确定读模型；014/018实现 | 04聚合粒度/字段/键、05区间汇总、06分类有效性、08读取身份 |
| B3 | R08两个子通道可能同键冲突，规则分类/客服可见范围未完整列出 | TASK-002/004前明确键与白名单；016实现 | 04实体键、07逐规则映射、02/08客服投影 |
| B4 | 关联数据源选择与配置改变的重算协议不闭合 | TASK-005前定来源组与配置行为；009前定提交检查 | 04/07/08的来源、覆盖、配置版本与更正影响 |
| B5 | 全店多版本快照无可执行的保留及引用清理边界 | TASK-013前；002读模型设计时一起考虑 | 04/10/11中热快照、冻结证据、在途引用与清理顺序 |
| B6 | 页面承诺失败重试，但部分JobRun没有明确恢复入口或ImportTask状态映射 | TASK-007/013前定接口及状态合同 | 04任务状态、08恢复动作、03按钮权限、09任务责任 |

### B1｜时间是计算输入，但没有进入版本协议

**证据：** 03 §5.2要求“每日观察窗更新”，06列出每日异常检测，07 R11在本地08:00判断缺覆盖，05要求D+7成熟度，04 §11.3却只列事实/标签与规则配置变更；DailyMetric/K2是不可变版本，Store只发布数据/规则身份。

**可复现的设计反例：** 没有新文件和新标签，昨天某队列未成熟，今天已达到168小时；同样，一份广告报告今天刚结束归因窗。按现有版本键，重跑会命中同一DailyMetric唯一键；若不重跑，成熟度永远停在昨天；若覆盖旧行，又违反不可变快照与历史日报要求。R11“新一天仍未导入”也不能只依赖导入事件触发。这是版本合同问题，不是简单“加一个cron”。

**推荐的最小方案：** 保留现有单一版本链，把dataset_version的定义明确扩为“分析输入版本”，允许本地每日固定评估事件推进它；不另建事件溯源系统。每日事件按店铺与本地评估日期幂等，在Store锁内确认本日未处理、推进版本并写现有Outbox。把本轮`evaluation_at`冻结在JobRun输入及发布元数据，重试复用，不在各指标函数中分别取当前时间；用一处店铺每日评估记录/字段保证重启不重复推进。导入、分类变更的计算也使用明确冻结的评估时点。

每日评估先重建确定性指标/规则，再按同一已发布身份生成日报；即使AI关闭仍评估成熟度和缺覆盖。今天的数据只显示“截至该快照评估时点”，不把浏览器此刻当作数据已计算到的时间。新版本使相关AI和查询缓存失效，历史报告保留原评估时间。

**关闭证据：** 固定同一批事实，推进时钟但不导入，D+7/广告成熟状态能更新；R11出现新日期缺失；旧报告不变化；同一天调度重投只推进一次；重建失败继续显示上一完整版。

### B2｜VOC和商品读模型没有覆盖页面实际需要的维度

**证据：** [VOCInsight/K3](</Users/yuyuyu/Documents/ChatGPT/产品-开发/docs/ai-ecommerce-assistant/04_DATA_MODEL.md:261>) 只有topic、period和版本，没有SKU、渠道、情感已知分母、关键词计数等完整读维度；08 `/voc`要求sku_id筛选，07 R09要求同渠道/分类版本比较；03要求FAQ、投诉、关键词、售后计数及样例，04又要求Dashboard/SKU/VOC聚合同版。CustomerMessage和SKU.product_id均允许更新。

**可复现的设计反例：** 快照V7统计SKU-A有10条负面消息。人工标签或来源更正已写事实V8，但重建尚未发布。若详情为支持sku_id而临时COUNT当前CustomerMessage，列表仍为V7、详情变成V8；若只读目前VOCInsight，又无法得到SKU筛选值。同样，拿V7的SKU销售数JOIN当前V8产品归属，会改变旧产品汇总。仅在响应里填dataset_version无法修复这种混版。

**推荐的最小方案：**

1. 先写一张现有页面所需的“输出字段→已发布来源/聚合粒度”映射。只补当前需要的日粒度、SKU（含未关联桶）、渠道、主题和情感/投诉/关键词计数；不做通用多维分析引擎或任意立方体。售后case原因计数也必须有对应快照输出；每条规则已完成或suppressed的原因/样本数保存在该次快照的确定性结果中，不能靠“没有Alert行”区分未运行和未触发。
2. VOC快照至少能按既有筛选确定性汇总，独立保存total、classified、sentiment_known、negative等分子分母，不能把每个主题重复存储的期间总数再SUM。pending/failed与“已完成但unknown”分开；聚合版本说明人工有效标签如何归入同一分类口径。
3. SKU到Product的聚合归属随快照固定，或直接保存所需产品聚合。商品当前名称可按原规范读取；参与统计的关系不能用当前可变关系替换历史关系。
4. 快照可点击样本保存最小脱敏文本、message_id与source_revision。标签编辑页可读取当前消息并明确“当前可编辑版本”，但不能将当前正文冒充旧快照原文。历史日报继续使用冻结证据。
5. 列表cursor包含数据/规则版本及筛选身份；翻页途中版本改变，明确返回VERSION_CONFLICT要求刷新，避免使用旧排序游标读取新榜单。无需为P0建立跨请求快照租约服务。
6. 区间指标仅汇总同版的金额/计数以及比例分子分母，再重算客单价、退款率、ROAS、负面率；不平均每日比例。每日规则和成熟队列告警按实际period过滤，不能把跨区间的最大/最小值当新规则。

**另一个必须明确的AI边界：** VOC分类是逐消息标签任务，应按message_id/source_revision、标签字典和人工修订做提交有效性检查；不能机械套用“任何店铺dataset_version变化都丢弃所有模型结果”。否则批次A提交标签使版本+1，会让排队中的批次B即使文本未变也全部过时，反复消耗额度。经营洞察/日报仍要求完整快照版本相同；两者在017/018明确分开。VOC结构错误仍按原合同整批拒收；结构已正确后遇到个别源修订失配，明确记录跳过并重新排队，只原子接受仍有效的标签且不覆盖人工结果。

**关闭证据：** V7→V8更新期间，首页、SKU/VOC筛选和样本要么都读V7，要么明确要求刷新；人工改标签只影响新快照；两个独立VOC批次可连续完成；消息已更正或人工已修改的结果不能被旧批次覆盖；范围汇总与手算一致。

### B3｜R08的两个告警必须有不同身份，客服授权不能依赖类别名

**证据：** 07 R08明确售后case数和显式投诉消息数独立计算、不相加；[04 K4](</Users/yuyuyu/Documents/ChatGPT/产品-开发/docs/ai-ecommerce-assistant/04_DATA_MODEL.md:294>)唯一键包含rule/entity/period/version，但没有子通道；entity_key当前只明确store/sku/campaign。两个子通道都用rule=R08、entity=store会同键。全套rule_id→category映射也未列清。

**最小处理：** 在现有实体键编码中明确R08两个固定子通道，例如`store:after_sales_case`与`store:complaint_message`，写入自然键、幂等、证据和API；R09按渠道计算也有明确固定作用域键。只增加当前规则需要的编码，不做可配置规则DSL。规则表补齐全部十条规则的category/entity/子通道/权限映射。

展示分类可以继续保持五类：R01/R02的店铺销量信号明确归入商品/销量类，不能假称导入故障；R05/R12广告类；R11数据完整性类；其余按商品、客服或售后分组。**客服最终授权按L1规则白名单执行**，R03/R10即使归为售后也不向C暴露订单/销量分母。R08两个通道同时触发应保存两条可解释告警，禁止覆盖或相加。

**关闭证据：** 相同日期两个R08通道同时触发、独立修改处理状态、重复计算不重复；C能看到授权case/投诉计数，不能通过售后筛选看到退款队列经营数据或备注中的经营内容。

### B4｜来源绑定及配置更改必须触发正确计算

**证据：** 04 §10.7允许每个source_kind/channel各自确定权威来源，而订单行、退款/售后关联又要求同namespace解析订单。08 Store PATCH能修改required_channels；R11依赖此配置，但没有对应Outbox/规则版本更新响应。显示名修改不改变经营事实版本的规则不能直接套到required_channels。

**反例：** orders绑定来源A、order_items绑定来源B，两边各自是“有效权威源”，但B中的订单行无法找到同namespace的订单。另一例是管理员把广告设为必需源，Store.row_version改变，旧规则/AI却继续称覆盖充分，因为没有重算。

**最小处理：**

- P0订单头、订单行和引用它们的case/refund使用同一明确的交易来源命名空间。不同原系统的文件可以由接入人员预处理为这套统一身份；不实现OrderAlias、动态来源迁移或自动跨平台对账。商品来源不同只走已有明确SkuAlias规则。源冲突在配置/预览即提示，不能等确认写入后再发现。
- required_channels的有效变化作为影响规则的配置修改，在同一事务推进ruleset_version、写JobRun Outbox并标更新中；不需要虚构新的订单事实。新快照发布前仍显示旧配置对应结果和更新提示，AI不能当作新配置已计算完成。纯显示名称修改不触发重算。
- 首次权威源绑定、空文件零事件声明和后续更正参与同店锁及审计。订单paid_at、订单行数量/金额更正时重验已存在的case/refund时间、金额和退件边界，不只检查本次CSV内部。付款日迁移后复核旧/新日期的订单行覆盖，不能沿用与事实冲突的explicit_zero或complete状态。

**关闭证据：** 不同交易namespace在预览安全拒绝；配置改动后旧AI可识别更新/过期；付款时间后移到现有售后事件之后被拒或明确修正，旧/新日期覆盖不被伪造。

### B5｜快照清理不能破坏证据，也不能只写一个N

在H2方案基础上，开发前补充四个可判定条件：

1. **热数据对象：** DailyMetric、VOCInsight、Alert及其发布身份是可清理的派生数据；订单、订单行、退款等事实不随热快照清理级联删除。覆盖历史清理不能删掉计算所需的最近有效声明。
2. **历史证据对象：** AIReport/AIInsight存储完整的必要证据投影、规则/指标版本、日期、数值、样例和解释；历史处理记录保留。若某个source_ref的显示仍需访问旧行，保留该行或先把必要内容归档入证据包，而不是只保留一个失效ID。
3. **清理安全顺序：** 当前与上一完整版本、在途构建及任务仍引用的输入不删。清理与发布/生成的引用登记使用同一数据库协调；不在发布事务中批量删历史。过时/失败构建先达到可清理终态，再分批清理。清理失败可重试并记录磁盘占用，不能影响现有页面读取。
4. **容量验证：** 029/030至少覆盖重复导入、有效VOC多批变更和清理后的占用/查询，不能只测一个初始版本。保留已有90天、10,000 SKU等验收规模；如实测不达，明确提出缩小试点数据边界，不能本轮默默降低标准或承诺100GB一定够。

P0无需存任意历史SQL可查询快照、无需让每个日报永久保留整店所有版本。它需要的是当前可用数据、可以解释的上一版与真实可读的历史报告。

### B6｜失败的“重试”必须能由有权用户执行

**证据：** 03多处承诺重试入口；04 ImportTask=committed只代表事实成功，后续计算失败在JobRun；08只有GET `/jobs/{id}`，而 `/imports/{id}/retry`没有说committed后哪一阶段可重试。规则配置修改、人工标签变化引发的重建甚至可能没有相应失败ImportTask。

**最小处理：** 补一张阶段恢复表：

| 状态 | 应有行为 |
|---|---|
| uploaded/validating出现可重试存储或解析运行故障 | 从保存的安全阶段恢复；结构错误要求修正文件，不自动重试坏行 |
| preview_ready但预览过期或受影响数据改变 | 重新校验并给新预览；重新确认，不能自动提交用户未见差异 |
| committing故障 | 查事务结果；已提交则返回既有结果，未提交才重试原子提交 |
| committed而rebuild失败 | 只重试重建，不重复写事实或递增导入版本 |
| VOC/规则配置导致的JobRun失败 | 有明确的现有任务恢复动作；旧版本输入已失效则标superseded，安排最新合法重建 |
| AI失败/预算不足 | 复用现有generate＋retry_failed或分类调度入口；保持attempt、限流和预算，不借任务重试绕过它们 |
| 原始或staging文件已清理 | 返回明确FILE_RETIRED/expired；提示重传，不显示无效“重试成功” |

建议在现有jobs资源上增加一个**受限的POST `/jobs/{id}/retry`薄入口**，复用原服务和幂等协议；只有发起人或有权管理员且当前仍有该操作权限才能执行，AI类再进入原预算/attempt门禁。不是新增工作流平台、任意job参数执行接口或新队列。若选择仅运维命令恢复，就必须相应删除普通用户的重试承诺、给出联系管理员说明；本报告优先保留现有自助恢复目标。

**关闭证据：** 已成功导入但重建失败时，用户可以恢复分析且金额/行数不变；规则重建失败也有可达路径；撤权用户不能重试；同任务重复点击只产生一次业务结果。

## 四、可以开发过程中处理的问题

以下不应成为“还不能开始环境搭建”的理由，但必须有任务责任和完成证据。

| 工作 | 处理时点 | 完成判据 |
|---|---|---|
| C1/C2/M2的既有裁决同步 | 第一轮规格整理；相关列表/配置实现前 | 默认分页25、规则阈值单一来源、唯一进度文件，合订本同步 |
| D4 Owner初始化与密码重置 | 003 | 干净库可初始化；重复执行不改密码；邀请单次/过期、身份一致性、撤权有效 |
| H4服务端限流 | 003/007/009/019/020；029复核 | 直接请求和重启后仍受保护；两种AI生成入口共享组织限制，429含可用等待信息 |
| 普通依赖选型、目录文件名、Adapter返回流形式 | 首次需要的TASK | 一个确定性规范化入口；采用流/有界批次，不能把100,000行无界装进对象数组 |
| H1保守处理状态延续 | 016/024 | 新日期/新证据不会被旧ignored遮蔽；不增加跨日静默、派单或通知体系 |
| Schema字段与DTO对齐 | 017/021/025 | 03的updated_by_user_id按唯一Schema改为action_states.user_id；不另维护一套AI Zod结构 |
| AI语义门禁的可实现边界 | 017/019 | 确定性校验ID、版本、数字、单位、引语及权限；自由文本假设明确标记；联网评测不能被stub代替 |
| VOC截断、分类缓存与队列优先顺序 | 018 | 保存截断说明/输入修订；500 code point坐标匹配；跨消息不复制引用；当前试用期间的待处理量可见 |
| 各API实际字段、稳定排序、错误/空状态 | 各服务与对应页面 | 区分missing/partial/zero/provisional、queued/running/failed、stale/updating；GET任务失败详情不误变为接口失败 |
| 只读审计查询、原文件/暂存清理、日志扫描 | 029 | 可查人/事/时间；按到期对象清理；日志没有客户全文、Key、Cookie、原模型响应 |
| 核心失败、性能和恢复验证 | 内核TASK就近测试；029/030汇总 | 不因文案归组跳过事务窗口、撤权或恢复测试；结果与执行环境可复现 |
| 企业实际文件适配及试用安排 | 尽量在006定模板时核对；030前完成真实样本验证 | 有原始导出字段→标准字段映射、接入工时和不可得字段说明，不能只拿合成CSV证明客户可用 |

AI语义校验不应演化成另一个“万能审查Agent”。最小做法是由服务端给出全部事实数字和引语，模型围绕这些受控证据写短摘要、假设与人工动作；数值/变化方向必须有明确证据对应，无法可靠判定的文本拒收或降级。不要仅因“有一个合法evidence_id”就放行任意经营断言，也不要承诺程序可证明所有自然语言结论为真。

## 五、P0应删除或延后的内容

以下是本次建议的减量边界，须用户确认后才修订原规格。

| 内容 | 处理 | 保留什么 |
|---|---|---|
| 五个重复AI Tab的独立页面组织 | 后置diagnosis/issues/priority等重复导航表现，P0用同一建议列表＋状态筛选＋详情 | 现有问题、证据、假设、动作、优先级和本人状态；首页仍显示最多三条重点动作 |
| 通用SkuAlias管理台、批量编辑、历史重绑定 | 后置 | 未知SKU报错后的明确映射和重验路径 |
| 数据源完整CRUD、来源迁移工具 | 本版不补齐“为了完整”的接口 | 创建、当前权威源/覆盖查看、店铺归档和服务端有效性校验 |
| 跨日告警静默N天、批量告警处理、告警任务流 | 后置，先取得真实重复噪声证据 | 当前版本告警处理及必要的同证据保守延续 |
| 跨消息语义缓存、自动主题合并、自由关键词发现 | 后置 | 同message/revision分类缓存、固定词表、确定性计数和人工标签 |
| 永久完整历史计算快照及任意历史重查 | 不作为P0能力 | 当前/上一完整快照、必要在途输入、冻结日报和证据 |
| 为每类失败各建独立测试框架、全样式镜像测试 | 删除重复建设 | 同一故障注入工具、关键事务/权限/Schema/恢复的有效断言 |
| Redis、BullMQ、微服务、事件溯源平台、向量库、RAG、Agent | 继续排除 | 一个PostgreSQL、已有pg-boss、一个Worker、Outbox与领域服务 |
| 平台自动同步、XLSX、跨店汇总/汇率、成本ROI、库存预测、自动回复/投放/调价、订阅支付 | 保持原P1/P2范围，不提前做 | 六类CSV、单店单币种、只读分析与人工行动 |

**不建议删掉的P0内容：** 服务端租户隔离和固定角色、私有文件、预览确认和整文件事务、业务自然键、权威来源/覆盖、金额/队列口径、Outbox及幂等、受控邀请、AI硬预算与降级、历史报告证据、真实PG与权限测试、最低备份恢复。它们直接决定企业试用的数据可信度，不应因“模型便宜”或“只是MVP”而去掉。

## 六、Zcode遗漏的问题

### 6.1 新发现与已有规定的衔接缺口

| 编号 | 遗漏/误判 | 具体影响 | 建议与责任 |
|---|---|---|---|
| N1 | 没有新导入时，成熟度和R11随时间变化的版本身份 | 旧成熟状态不更新，或同键覆盖不可变数据 | B1；002/013/015/020 |
| N2 | VOC按SKU/渠道筛选、关键词/情感分母和售后摘要缺完整快照读模型 | 页面临时读当前事实会混版；只读现表又缺维度 | B2；002/014/018/021/023 |
| N3 | R08两个子通道同键，R09渠道作用域未编码 | 告警覆盖、唯一键异常或将两个计数混合 | B3；002/016 |
| N4 | “after_sales去金额即可给客服”是错误权限近似 | R03/R10仍可能泄露订单/销量和相关正文 | L1/B3；004/016/019/024 |
| N5 | required_channels修改未明确联动规则版本和Outbox | 配置已变而覆盖结论/AI仍旧 | B4；005/009/013/016 |
| N6 | 交易各类型独立选源与同namespace外键存在接入陷阱 | 每个来源都合法，但订单行/售后无法关联 | B4；005/006/008 |
| N7 | RR发布遇40001时的状态写回、事务边界与饥饿处理未落实 | 可能反复失败或在已回滚事务中错误写状态 | M4；013/016。已有概念协议不能当可运行实现 |
| N8 | 已committed导入的分析失败、非导入触发JobRun失败没有完整恢复API合同 | 重试按钮不可用，或重复提交事实 | B6；007/013/024/026 |
| N9 | VOC逐消息有效性与全店版本校验边界未区分；跨消息复用文字冲突 | 前一批分类让后一批无关结果失效；错误复用span | B2及06 §24.2优先；修09 TASK-018“重复文本可复用”表述 |
| N10 | 约9元/月仅按1次日报调用估算，未充分枚举insight/CS/多版本生成 | 平均估算不能证明实际成本或支持删除硬预算 | 06 PART24补按kind/scope/次数的估算；019/020记录实际调用，017仍保留上限 |
| N11 | 首家企业真实导出与标准模板的可获得性未经核对 | 标准模板可能要求客户拿不到的稳定行ID、累计退件数、时间/币种或拆表信息 | 006前尽早做人工字段核对；030前跑至少一组真实脱敏样本，不开发通用自动清洗平台 |
| N12 | 03字段表写updated_by_user_id，唯一Schema只有actionState.user_id | 前端按表写字段会被additionalProperties=false拒收 | 03 §4.6改名；017/025从唯一Schema取类型 |
| N13 | 变更中的分页/证据钻取与Product当前归属可能混版 | 重复/漏行、旧指标被分入新产品、旧原文漂移 | B2；008/021/022/023/025；cursor带版本并处理刷新 |
| N14 | 订单paid_at更正后，已有售后时间和覆盖声明的重验未明确到任务 | 可能出现售后早于付款、已声明零日实际有订单行 | B4；009/010/012/015的关联重验案例 |
| N15 | 限流已有TASK，词表已有规则，阈值已有Store.settings限制，复合关系并非一律raw SQL | 误导开发者进行不必要决策或工程改造 | 按H4/M2/L2/L6纠正，不增加额外架构 |

N10的价格与调用量需分开：本次官方价格表仍列出该模型相应档位输入0.15元、输出1.5元/百万Token，原表算术基本成立；但它没有证明实际会只发生估算中的调用次数，也没有核实用户账号可调用性。[百炼模型价格](https://help.aliyun.com/zh/model-studio/model-pricing) 不自动换模型，不因官网列出模型就把017的实测记为通过。

N11需要特别区分**工程和接入**：04已允许接入人员在上传前拆订单头/行、按订单行分摊整单退款，并提供可信的人工导出版本时间。因此，不应强行新增第七类文件或平台自动适配；应核对这些人工准备步骤对首家厦门企业是否做得到、需多少时间。缺少关键字段就暂停该指标或更换合适试点样本，不能编造ID、时间、退件量或complete声明。

### 6.2 用户指定检查域的完整检查结果

| 检查域 | 已有合理约束 | 剩余问题及本次判断 |
|---|---|---|
| 数据导入 | 六种CSV、流式解析、上限、全量校验、预览确认、整文件原子性、错误下载 | 交易来源绑定、关联更正重验、已提交后的恢复按B4/B6补齐；实际导出适配未验证 |
| 指标口径 | GMV只算订单行；付款订单去重；事件日退款与D7分开；ROAS不当ROI | 保留；补区间聚合、稀疏零值与每日评估身份；比率比较使用未舍入分子/分母 |
| 多店铺数据隔离 | org/store复合约束、请求/worker/缓存/文件/AI鉴权 | 保留并实测两种情形：同组织不同店防混算、不同组织防越权；本版不提供同组织内店铺级成员授权 |
| 时区 | IANA、UTC事实、半开区间、店铺时区、连续168小时 | 设计方向完整，缺的是时间推进后的版本/评估协议；不把固定UTC cron当所有店本地08:00 |
| 币种 | 单店单币种、Decimal、API金额字符串、计费CNY独立 | 合理；保留币种拒绝、精度和上界测试，R05/R12金额必须同店币种配置 |
| SKU唯一标识 | 稳定外部ID、namespace、店内sku_code、显式别名 | 合理；同名不合并；别名恢复入口及产品归属快照按O2/B2补齐 |
| 数据去重 | HTTP键、文件hash/映射/覆盖键、业务自然键、任务幂等 | 保留；不以文件hash代替行幂等，不因source_updated_at或缺行重传删事实 |
| 异常规则 | 十条P0、样本/覆盖/成熟门槛、suppressed、规则版本 | 需R08子通道键、完整category/授权映射、R11时间触发；H1只做保守延续 |
| AI JSON Schema | envelope/payload分工、三个LLM入口、Ajv/format/语义校验 | 结构方向合理；正文与Schema字段对齐、字段级语义反例不能省；本轮未执行Ajv或模型评测 |
| AI失败降级 | failed/skipped＋payload null；规则摘要标source，核心数据独立 | 保留；无证据时允许不生成；generic job retry不能绕过attempt/预算 |
| VOC数据链路 | 逐消息分类、人工优先、unknown分母、投诉显式标记、code point span | 需已发布维度和样本、消息修订级有效性、截断标识；不把部分分类比例当全量 |
| Dashboard来源 | 已发布指标/规则/VOC同版；updating/failed/stale区分 | 不能只声明同版，要补每个面板字段的真实聚合来源，禁止联查当前可变事实冒充旧快照 |
| Data Adapter | CSV/Mock统一标准记录、来源只做转换、不直接写UI | 保留；CanonicalBatch/NormalizedBatch名称统一为一个正式契约，records实现为有界流，不做插件市场 |
| ImportTask | 状态持久化、staging/预览版本、Outbox、原子提交、24h预览 | 补阶段恢复和expired落点；业务提交与分析/AI状态必须分开显示 |
| 权限 | 四固定角色、受控邀请、Admin边界、服务端全链路校验 | 主要新增是L1；邀请接受时复核当前授予权限，成员禁用/组织暂停/店归档影响后台执行 |
| 错误状态 | 稳定码、request_id、安全提示、重试有限 | L5/B6补HTTP与后台阶段映射；无权限不泄露对象存在性 |
| Empty State | 无组织数据、无店铺、无源、零值、历史不足、AI失败均有规范 | 基本齐全，不新增引导系统；细化“数据准备中”与“服务故障”，不把无快照统一当503 |
| 测试 | Unit/真实PG/E2E/Schema/联网AI/性能/恢复分层 | 保留既定门槛，缩减重复实现；新反例并入原TASK，不额外建第二套测试架构 |
| 日志 | Pino、request/job/import关联、正文/凭据屏蔽、滚动保留 | 029补可操作查询/清理证据；文件名、备注、模型错误也按不可信文本处理 |
| 企业数据安全 | 私有存储、最小出站证据、隔离、保留/清理、异机备份 | 试点前核实企业允许的供应商数据用途、保留期和删除范围；不扩成合规平台，不宣称已完成安全认证 |
| 未来扩展 | Adapter、来源身份、稳定业务键、只读建议和外部执行分离 | 保留边界；不为未来平台预建OAuth、向量表、ExecutionCommand运行系统或自定义权限引擎 |

另一个实际试用风险是分类积压：验收规模含每店50,000条消息，而默认每日2,000条分类；即使没有新增，也需要至少25个配额日，不能承诺一次导入立刻得到全量VOC。018优先处理当前试用关注期间、记录剩余量和覆盖；初始样本范围与额度由试点计划明确，不自动提高费用或把待分类量隐藏掉。

## 七、最终建议P0范围

P0仍然是一个**单店经营晨报与问题复核闭环**：企业人员按明确模板导入数据，查看确定性指标和异常，结合脱敏VOC判断下一步，记录本人处理状态，再观察是否再次使用和接受报价。不是自动经营系统，也不新增平台执行动作。

| 范围 | 最终建议 |
|---|---|
| 试点对象 | 1—2家能提供稳定身份和必要导出、愿意实际核对的厦门电商企业；每次试点一个店、一种币种、一位业务负责人。该试点安排不降低原工程隔离验收 |
| 身份/权限 | 初始化Owner、受控邀请、四角色、组织与店铺归属校验；同一组织角色默认覆盖全部店铺。需要员工店铺级隔离的企业不使用此P0权限模型 |
| 数据输入 | 六类CSV及同链Mock；产品→订单头→订单行；广告、客户消息、售后按来源能力导入。某店缺可选源允许缺失/停用相应指标，但工程仍验证全部六类 |
| 数据正确性 | 确定性GMV/订单/销量/客单价/退款/成熟队列/广告；固定时区币种、自然键、覆盖与版本；不计算未知成本/库存/流量 |
| 问题发现 | 十条既定P0规则及条件停用；同口径历史、样本、成熟度、规则证据；无配置不擅自启用广告目标告警 |
| VOC | 有限主题/情感分类、固定关键词、确定性计数、脱敏样例、人工标签；部分覆盖和截断明确可见 |
| AI | 一个固定模型，三类已有输出入口：VOC批分类、洞察、日报四结论；统一网关/预算/校验；无外部工具，失败核心页面仍可用 |
| 页面 | Dashboard、经营指标、SKU列表/详情、客服三Tab、告警、单一建议列表/详情、日报历史、导入向导/恢复、必要设置；减少重复AI导航，复用服务和组件 |
| 人工状态 | 当前用户自己的accepted/done/dismissed等状态；告警处理独立。完成只代表用户记录，不代表系统执行或业务效果成立 |
| 试点运行 | HTTPS、私有文件、受限日志/审计、已有队列/Outbox、费用上限、到期清理、异机备份与实际恢复，基本任务运行状态 |
| 商业验证 | 记录接入/核数耗时、真实再次打开、采用动作、报价接受与付款；报告生成、Demo通过和软件部署均不能代替这些证据 |

**建议保留原固定技术栈。** pg-boss在这里承担CSV后台处理、确定性重建、固定日报和可恢复任务，已有实际用途，不属于为了“完整”而新增的基础设施。需要控制的是重复实现和无边界历史展开，不是把任务移回Web请求中或删除持久队列。

## 八、最终推荐TASK顺序

### 8.1 先关闭合同，再按单任务推进

本报告没有给项目新增TASK编号。首先由用户确认本次审查结论及减量建议，再对第九节列出的文档做有范围的修订；**本轮不执行修订或开发。** 001可单独授权先做环境；进入002前必须确定B1/B2/B3影响的模型合同。其他门槛按第三节最晚截止点执行，避免要求提前完成全部业务实现。

### 8.2 推荐顺序

| 阶段 | 顺序 | 阶段结束应得到什么 |
|---|---|---|
| 本地环境 | **001** | 可启动Web/Worker/PG及真实检查；不建业务实体或页面 |
| 身份和数据库 | **002→003→004** | 修订后的P0模型/约束、Owner和邀请、两组织四角色授权入口 |
| 提前暴露AI外部风险 | **017** | 单模型网关、结构/候选证据校验、原子预算、真实PG及一次脱敏真实模型合同检查；不代表最终AI质量通过 |
| 导入基础 | **005→006→007→008→009** | 来源、标准模板/黄金样本、私有文件和ImportTask、可审查预览、原子提交内核 |
| 六类事实落库 | **010→011→012** | 订单头/行、广告、客户消息、case/refund通过统一链写入 |
| 确定性分析 | **013→014→015→016** | 持久恢复、金额与覆盖、成熟队列、十条规则和完整可读快照 |
| VOC与解释 | **018→019→020** | 可核验分类及快照、真正的EvidenceBuilder、洞察、固定日报和降级 |
| 使用路径 | **021→022→023→024→025→026→027** | 页面共用真实数据服务；老板和客服路径、个人动作、导入恢复、必要设置可操作 |
| 演示与试点交接 | **028→029→030** | 从空库重建演示、部署/备份恢复/最低运维、完整测试及真实企业接入记录 |

调整仅是017提前，其他依赖主干保留；已静态检查该建议序列包含全部30个TASK且满足改后的依赖。需要同步handoff §6、09与12三张表。不要保持一张旧表、再让Coding Agent自行猜哪个优先。不存在“提前017自动节省一个TASK工时”的承诺。

### 8.3 任务执行要求

- 同时只有一个TASK为IN_PROGRESS；H3的内部阶段不等于新增TASK或跨任务并行授权。
- 008先公共内核再六种契约；016按可复用规则内核和触发/不触发/边界测试推进；021先确定性数据与各种状态，再接AI；026最后必须完成可恢复的完整导入路径。
- 006保留小黄金样本，028扩充28天可演示数据；不得到028才首次核对GMV、退款、店铺隔离。
- 017的业务证据输入测试可使用真实PG中的合成记录；019必须使用实际指标/规则/VOC服务的结果重新验证，030仍执行独立联网评测。
- 013先交付骨架，但在014—016完成前不能把它写成“完整分析链已发布”；021完成不等于026企业可自行导入，028演示不等于030真实试点可用。
- 原文20—30工作日仍只是估计。先观察001—009实际耗时与首家导出准备成本，再更新排期，不承诺几天内完成整个MVP。

## 九、需要修改的文档

下表是**待确认的修订清单**，不是本轮已经完成的文件变更。修改以分册为真源，再同步合订本；不要重写全部项目或把报告内容整段粘进每个文件。

| 文件名 | 修改位置 | 具体修改内容 | 对应问题 / 截止 |
|---|---|---|---|
| `DEVELOPMENT_HANDOFF.md` | §2.3、§5.2/5.4、§6、§7 | 若采纳减量，AI工作台改为单列表/详情；更新每日评估也是版本输入、客服告警边界和恢复原则；017依赖/顺序同步；说明ZIP归档身份 | S0、B1/B3/B6、M1；业务开发前同步 |
| `01_PRODUCT_VISION.md` | §1.4/1.5、PART19 | 保持业务闭环和P1/P2排除；明确重复AI Tab后置；列真实导出适配与人工准备成本为试点验证输入，不声称模板即可接全部企业 | S0、N11；页面开发前 |
| `02_USER_ROLES.md` | 权限矩阵及客服投影段、邀请段 | 列出C可见规则/子通道；R03/R10不返回；备注/标题/样本同样受限；邀请接受和任务恢复检查当前权限 | L1/B3/B6；004前 |
| `03_INFORMATION_ARCHITECTURE.md` | PART3、§4.1/4.3/4.5/4.6、§5.2、PART18 | 给现有Dashboard/VOC字段标明快照来源；补同版样本/更新与恢复状态；action_states字段用user_id；缩减重复AI导航；未知SKU映射仅嵌入错误恢复 | B1/B2/B6、N12、S0/O2；相应页面前 |
| `04_DATA_MODEL.md` | Store/RuleConfig/CustomerMessage/DailyMetric/VOCInsight/Alert/JobRun/ImportTask、§10.7、§11.1—11.3、§12.3/12.4/12.7 | 修settings摘要；补evaluation_at/每日评估幂等与版本语义；明确VOC/SKU/Product必要快照维度和计数分母；R08子通道键；交易来源组；配置Outbox；关联更正重验；阶段恢复；快照/证据保留与清理；Prisma约束表达说明 | B1—B6、M2/L6；模型部分002前，后续合同按第三节截止 |
| `05_METRIC_DEFINITIONS.md` | §6.1共同口径、派生量/产品聚合说明 | 冻结计算as-of与168小时成熟；区间汇总分子分母；稀疏零值必须有目录/覆盖依据；VOC已知情感分母和各计数范围明确 | B1/B2；014前 |
| `06_AI_CAPABILITIES.md` | §8.2/8.3、§9.3/9.4、PART24 | VOC按消息修订提交，洞察/日报按完整快照校验；样本/截断/人工标签版本；固定severity→priority映射及失败行为；解释可确定性验证的事实边界；按kind/scope/频次补成本假设；保留预占attempt合同 | B1/B2、N9/N10、M3；017前相关校验合同，019前编排 |
| `07_ALERT_RULES.md` | 共同门槛、十条P0映射、R08/R09/R11、末尾状态段 | 补rule→category/entity/subchannel/CS白名单；每日评估；required_channels改动的规则版本；同证据保守状态延续；基准零时不得生成无穷相对变化 | B1/B3/B4、H1/L1；016前 |
| `08_API_SPEC.md` | §17.1—17.4 | 行级错误引用与异步失败示例；cursor带版本/筛选身份；覆盖要求变化返回重建身份；补现有jobs的受限恢复动作与权限；committed后重建重试、expired/retired行为；客服字段白名单 | L5、B2/B4/B6；相关API开发前 |
| `09_TASKS.md` | 总执行规则；003/005/007—020/021/024—030 | 允许同TASK跨会话；限流细分归属；Owner/重置约定；将B1—B6的验收反例放回所属TASK；017只依赖004并前移；修018跨消息复用文字；减025重复Tab；029保留必要测试/只读审计/清理 | H3/H4/M1等；对应TASK前 |
| `10_ACCEPTANCE_CRITERIA.md` | PART13、§21.1、PART22/23 | 默认分页25；加入无导入推进时间、R08双通道、V7/V8钻取、required_channels改变、已提交后失败恢复、重复版本清理后历史阅读等反例；保持原性能/AI/权限/恢复门槛；明确保留对象与期限来源 | C1、B1—B6、O3；内核开发前定反例，发布前执行 |
| `11_DEVELOPMENT_RULES.md` | 第5/7条、配置迁移段、§14.2/14.3、PART16 | 明确同TASK跨会话、唯一规范/进度；删除复制正文及应用内docs目录说明；补评估时点、RR/CAS重试、全店并发及清理协议；保留单库/单Worker/原计费机制 | C2、H3/M4、B1/B5；001入口及相关内核前 |
| `12_PROGRESS.md` | 依赖表、记录格式、实际验收状态 | 用户确认后同步017依赖；记录各阻塞合同是否已修订及真实测试。未运行TASK继续TODO，审查通过不能改成DONE | M1、G2；实际执行时 |
| `00_COMPLETE_SPEC.md` | 对应PART与PART26 | 从修订分册同步；删除独立可编辑副本的复制指令；不把合订本当第二规范真源 | C2及其他已确认修订；最后同步 |
| `AI电商运营助手_产品与工程规格_v1.0.zip` | 归档包 | 全部分册/合订本修订并检查后统一重打包，注明版本，避免旧包覆盖新规格 | 交接一致性；最后处理 |

不要求修改Zcode原始诊断报告；保留它作为被审查版本。本独立报告已给出逐项纠正，便于对照责任和决策。库的具体补丁版本、常量文件名和通用组件拆分不需要再扩成产品文档。

## 十、最终Go / No-Go

**现在能否开始TASK-001？——技术上可以，限定为环境搭建；本轮不开始。**

| 对象 | 结论 | 原因 |
|---|---|---|
| TASK-001：本地Web/Worker/PostgreSQL和检查环境 | **GO，待用户明确启动** | 本报告的问题均不要求在该任务建立业务表、接入AI或开发页面；已有执行包足够具体 |
| 原样启动TASK-002并持续完成全部业务开发 | **NO-GO** | 至少先确定B1/B2/B3的模型、版本、规则身份与权限合同；其余在各自门槛前关闭 |
| 在指定契约修订后按本报告顺序推进P0 | **GO** | 保留现有技术栈和闭环，范围收敛，修订可落在现有TASK内 |
| 宣称“工程已完成”“真实企业可用”“AI质量通过”“已验证付费” | **NO-GO** | 当前没有应用、运行环境、真实数据/AI/性能/恢复或商业结果证据 |

下一阶段应是对本报告的具体修订和减量建议作确认，再执行选定范围。若仅授权001，完成它后先交付真实环境检查结果，不顺手继续002。若先授权文档修订，只改第九节确认的内容并复核一致性，不因此自动进入完整开发。

---

**本轮实际交付核验：** 新增本独立审查报告。原DEVELOPMENT_HANDOFF、00—12分册/合订本、ZIP和Zcode诊断文件未改；全部开发任务状态保持原样。本报告中的设计反例和验收建议是后续需要执行的检查，不标记为已运行的软件测试。
