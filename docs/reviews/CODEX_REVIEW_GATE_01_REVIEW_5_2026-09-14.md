# CODEX REVIEW｜GATE 01

Reviewer：Codex｜2026-09-14｜REVIEW_5 收敛复审｜Phase 1 / TASK-001–004。

## 1. 最终结论

**PASS（独立技术审查通过）。Owner阶段放行尚未获得。**

本轮冻结为`32fb0d3e8ad19b691cf66006638a418ca949e2a4`，分支`phase/01-foundation`，实际远端与本地一致；main仍`2a983cc55f136abbb49c5d02b55c1cb82b6547cc`。主要差异`858c20a..32fb0d3`：13个应用实现/迁移/测试文件，另24个ZCode容器证据文件在应用目录内。开始工作区干净。

按P07和事先确定的收敛约定独立检查，**H12、M03、M04、M06、M07关闭，无未关闭CRITICAL/HIGH/MEDIUM**。保留一项不阻塞当前版本的LOW兼容性提示。M07按已约定的“自定义SQL明确维护、行为回归保护”验收，不代表Prisma自动生成的重建SQL可直接应用。

代码检查与运行均基于该候选的Git归档。使用独立PG17.11集群、一次性测试库、临时Web及独立Compose项目；没有修改原应用、Schema、迁移或测试，没有操作原开发库。测试、审查、Owner、GitHub与线上部署分别记录。

证据：[机器索引](/Users/yuyuyu/Documents/ChatGPT/产品-开发/docs/reviews/GATE_01_REVIEW_5_EVIDENCE_2026-09-14.json)、[日志与复现说明](/Users/yuyuyu/Documents/ChatGPT/产品-开发/docs/reviews/gate-01-review-5-evidence/README.md)。原R4及更早报告保持历史，不用本轮结果覆盖当时失败证据。

## 2. TASK验收

| TASK | 结论 | 依据 |
|---|---|---|
| TASK-001 | PASS | 锁定安装、Web/Worker构建、真实容器两种口令完整链路、健康200→503；未变的缺配置/退出历史证据继续适用。 |
| TASK-002 | PASS | 时间绝对值、28张领域表UUID约束、真实10迁移/8→10升级/重复/坏行拒绝、迁移目标边界、审计并发/删除及Schema维护边界通过。 |
| TASK-003 | PASS | 过期邀请不签发会话，有效邀请可登录；限流/会话数据库错误稳定返回；登录/退出、禁用、并发与原子回滚通过。 |
| TASK-004 | PASS | 四角色、多组织、权限投影与D01回归通过，依赖的TASK-002/003本轮已通过；未来文件/Job/AI完整链路仍在所属TASK验收。 |

## 3. CRITICAL

**NONE。**

## 4. HIGH

**NONE（未关闭项）。H12本轮关闭。**

H12位置为[src/database/prisma.ts:23](/Users/yuyuyu/Documents/ChatGPT/产品-开发/ai-ecommerce-assistant/src/database/prisma.ts:23)及既有邀请时效检查。独立默认Asia/Shanghai数据库中：

- 四次正式HTTP创建邀请，响应到期epoch与数据库均相差0ms；SQL写入历史时间后，ORM读数差小于1ms，为PG微秒到JS毫秒精度截断。
- 创建49小时前、TTL48小时的邀请：先预览410后接受409；直接接受410。到期边界的直接接受也为410。均不签发Cookie、不新建Auth用户。
- 创建47小时前、还剩1小时的有效邀请：接受200、Cookie签发、`/me`200。
- 三组连接参数（数据库默认、显式UTC、显式America/New_York），每组同时占用6条不同PG连接：应用池均为UTC，归还再取仍为UTC。另一个默认UTC的独立库，ORM写入与原始SQL epoch差0ms。
- 原生认证会话有效时`/me`200，按UTC语义置为过期后401；有效/过期判定未被放宽。
- 两次真实Linux容器验证也在非UTC数据库上得到创建epoch差0ms、过期直接接受410且不签发Cookie。

**连接范围核定：**当前持久化领域数据及实际认证入口使用修复后的Prisma路径，Owner初始化/改密脚本也调用该工厂。Worker目前只经原始pg执行健康检查，仍使用数据库默认时区；独立原始pg Date/epoch差0，且此路径不写领域时间。`auth-schema.config.ts`只作表结构生成，并非运行时认证。因而不沿用交接“所有Worker/CLI连接均UTC”的过宽表述；上述差异未构成本轮H12反例。未来Worker新增业务必须按对应TASK核对其实际数据访问路径。

## 5. MEDIUM

**NONE（未关闭项）。四项处理均已独立核定：**

| 编号 | 实际验证 | 核定 |
|---|---|---|
| M03 | 注入限流表写入失败，预览/接受均503 JSON；使会话表临时不可读后，创建/列表/接受/撤销均503 JSON且含request_id，邀请数量不变。审计回滚、非法输入及日志request_id对应亦通过。 | PASS，当前入口边界关闭。 |
| M04 | 数据库目录枚举28张领域表均有已验证、作用于id的UUID CHECK；辅助auth_rate_limit单独约束，框架四表保持原生。非法JobRun写入拒绝、合法值通过；旧8迁移库含非法JobRun时升级P3018拒绝。 | PASS，不继续延期。 |
| M06 | 独立新库首次到第4迁移恰好4份；重复执行0份；不存在目标明确报错。辅助器全量后官方CLI无待执行迁移。 | PASS，目标边界与正式CLI兼容成立。 |
| M07 | Schema已声明同域复合引用，已提交迁移保留`SET NULL (store_id)`；H08双向真实锁等待后均拒绝冲突、cross_org=0，删除只清store_id。生成差异与自定义SQL差异已单独实测如下。 | PASS，按事先允许的自定义SQL维护方案关闭；不宣称生成器完全表达该约束。 |

**M07必须保留的维护约定：**当前官方diff仍输出DROP+ADD。该生成SQL将按列SET NULL变成全列SET NULL，并把ON UPDATE NO ACTION变成CASCADE，不能称为“等价重建”。在独立一次性模拟库中执行该SQL后，删除店铺报`23502 / org_id`；当前已提交迁移下相同删除成功、org_id保留。这证明H08行为回归可以检出错误重建。

以后涉及该关系的迁移必须人工检查并保持已定删除/更新语义，再跑H08行为回归；不能只看DROP与ADD数量配平。此边界在收敛约定中已允许通过明确维护方式处理，故不重新把它升级为当前阻塞项。生成SQL仅在一次性模拟库执行，原工程、主测试库和开发库未应用。

## 6. LOW

### L01｜连接初始化产生pg查询排队弃用提示

- **问题：**连接建立时的SET查询与紧随其后的查询排队，锁定的pg8.23.0在E2E/HTTP运行中输出弃用提示；当前结果均通过。
- **位置：**[src/database/prisma.ts:30](/Users/yuyuyu/Documents/ChatGPT/产品-开发/ai-ecommerce-assistant/src/database/prisma.ts:30)。
- **证据：**`e2e.log`、`http-probes.log`中的DeprecationWarning，提示未来pg9将移除当前排队方式；本轮18条并发连接及实际请求仍正确。
- **风险：**后续升级驱动时可能不兼容；没有在锁定版本复现请求失败或时间偏移。
- **建议：**在升级pg主版本前明确等待连接初始化完成，保留每条连接的UTC验证。当前不为此追加Gate返修，不要求无关重构。

管理文字中的“P0无部署要求”、旧首页HEAD和ZCode证据根路径在本轮交接中校正，历史原文保留，不列为业务缺陷。

## 7. 数据库审查

**PASS。数据库层技术上允许支撑下一Phase，但实际启动仍需Owner阶段放行。**

官方CLI空库10迁移、重复deploy、858c20a的8份旧迁移升级到10份、再次deploy均通过。旧库合法样本原时刻保留；非法UUID旧行按预期拒绝。更早未改迁移/坏审计/身份守卫证据沿用R4，当前完整集成仍覆盖相关守卫；未改写历史迁移或盲目平移真实数据。

28张领域表主键约束、91个领域timestamptz列与当前访问语义通过。M07的特殊外键维护约定及真实历史数据来源核验继续保留。

## 8. Auth / 权限 / 多租户审查

**PASS；本轮是否复现跨Organization数据读取泄漏：NO（限已实现接口）。**

实际HTTP覆盖组织切换/伪造上下文回退、CustomerService金额字段投影与写入403、Admin不能授Admin/Owner、合法授权、邀请会话/重放、公开注册403、Owner并发初始化、审计失败完整回滚、旧Cookie撤销和退出后401。D01方案A保持：只禁本组织Membership，全局User不受组织接口禁用，其他有效组织重登可用。

对本轮3个提交可达420个不同blob进行强模式扫描，未发现强密钥/Cookie候选，真实.env没有跟踪。该扫描不等于对所有文本秘密的绝对保证；测试数据与生产数据分别看待。

## 9. 测试审查

| 本轮独立执行 | 结果 |
|---|---|
| 锁定离线安装及Prisma generate | PASS，278包；构建脚本提示保留，generate确实执行 |
| Typecheck / Unit | PASS / 18/18 |
| Integration | 65/65，7套件；0未捕获错误；旁观连接和跨全套件的在途事务完好，冲突.env不覆盖注入配置，测试库无遗留 |
| Web/Worker build | PASS |
| E2E | 首次8/8；保留pg弃用和颜色变量警告 |
| 独立数据库/HTTP补充 | 五项关闭矩阵与H08/D01等回归通过，详见索引 |
| 官方迁移 | 空库10、8→10、重复、辅助器接续通过；坏行拒绝符合预期 |
| Docker | 纯Git上下文真实构建通过；允许标准层缓存，耗时约386秒；默认/随机特殊字符口令各用全新卷、非UTC数据库完成完整链路 |
| 容器完整链路 | up→健康200→10迁移→Owner初始化→登录/me200→公开注册403×2→邀请epoch一致/过期410→Worker检查0→down --volumes |
| 健康降级 | 临时PG在线200，停止后503 |

现有测试可信，关键原反例已成为回归；独立补充验证覆盖精确时刻、直接接受到期邀请、并发池连接、会话读取故障和Schema生成差异，避免仅依赖新增用例数量。

执行偏差：第一次复用Reviewer HTTP脚本遗漏`REVIEW_WEB_LOG`环境参数，完成前面探测后在日志对应检查处失败；该错误与部分结果保留为`http-probes-setup-error.*`。补齐脚本参数、清理本次临时限流记录后完整重跑exit0。未将此准备错误算为产品缺陷。没有重新诊断iCloud根因，也没有把ZCode的假定日期/路径当成独立证据。

未运行未来CSV/Job/AI/文件端到端、生产负载、真实企业试用或线上部署；这些不是本次Phase1范围。更早未变的进程退出实验和运行边界证据保留，不重复无关实验。

## 10. P0范围检查

**超范围实现：NO。** 两个新增迁移、连接池及测试/异常边界改动均归当前TASK；没有新增大型依赖、产品能力或TASK-005实现。

## 11. 过度设计检查

**需要整改的过度设计：NO。** 保持单应用、单PostgreSQL和Web/Worker结构；没有为本轮复审建设新的测试平台。

## 12. GPT_PRODUCT_DECISION_REQUIRED

**NONE。** D01保持；技术维护边界已核定，不需要重新询问产品规则。Owner尚需做阶段放行，这与新增产品规则裁决不同。

## 13. Zcode必须修复项

**CRITICAL：NONE；HIGH：NONE。** 本轮不再交回新的阻塞修复清单。M07维护约定和L01升级前注意事项保留，不能把它们写成无需遵守。

## 14. 是否需要Codex复审

**NO（同一冻结版本无需再次复审）。** 若此后修改影响已审行为、依赖、迁移或配置，按实际差异复核；不能让本次PASS覆盖未来未审代码。

## 15. Gate结论

- **技术Gate：PASS，适用于32fb0d3。**
- **现在是否允许执行phase/01-foundation→main合并：NO，尚缺Owner阶段放行。** 技术前置条件已满足。
- **现在是否允许开始Phase2/TASK-005：NO，仍等待Owner明确授权。**
- 当前Phase1/TASK-004保持Checkpoint=YES，下一责任人为Owner；不再让ZCode循环修复已关闭问题。

本轮只完成独立Review和原管理文档维护，没有提交、推送、合并或部署。收尾的文件校验、环境清理和Product OS sync/首页与总控读回记录在唯一进度及机器索引中。
